FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY orchestrator/ ./orchestrator/

EXPOSE 8003

CMD ["uvicorn", "orchestrator.engine:app", "--host", "0.0.0.0", "--port", "8003"]
